name: CI

on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.18.0
          cache: npm
      - name: Install exact dependencies
        run: npm ci
      - name: Validate the installed dependency tree
        run: npm ls --all
      - name: Build the first-party seat relay container
        run: docker build --tag railscout-relay:test .
      - name: Verify source, build, tests, data, and licenses
        run: npm run check
      - name: Reject high-severity dependency advisories
        run: npm run security:audit
